{"actor":"MRiless","comment":null,"created_at":"2026-03-28T10:19:06Z","event_type":"created","id":1,"issue_id":"prompt-strength-8g6","new_value":"","old_value":""}
{"actor":"MRiless","comment":null,"created_at":"2026-03-28T10:19:26Z","event_type":"created","id":2,"issue_id":"prompt-strength-bz1","new_value":"","old_value":""}
{"actor":"MRiless","comment":null,"created_at":"2026-03-28T10:20:54Z","event_type":"updated","id":3,"issue_id":"prompt-strength-bz1","new_value":"{\"design\":\"## Goal\\nCreate Railway configuration so both services deploy from the same repo, and update nginx to proxy to the backend via Railway internal networking.\\n\\n## Implementation\\n\\n### 1. Update nginx.conf to use templated backend host\\n\\nConvert `frontend/nginx.conf` to `frontend/nginx.conf.template`:\\n- Replace `http://admin:3001` with `http://${BACKEND_HOST}:${BACKEND_PORT}`\\n- **CRITICAL:** Keep all nginx variables ($uri, $host, etc.) as-is — envsubst must only target our variables\\n\\n### 2. Update frontend Dockerfile for envsubst at runtime\\n\\nAdd to frontend/Dockerfile:\\n- Set default env vars: `BACKEND_HOST=admin`, `BACKEND_PORT=3001`\\n- Replace CMD with: `envsubst '${BACKEND_HOST} ${BACKEND_PORT}' \\u003c /etc/nginx/conf.d/default.conf.template \\u003e /etc/nginx/conf.d/default.conf \\u0026\\u0026 nginx -g 'daemon off;'`\\n- **CRITICAL:** The envsubst command MUST specify exact variable names to avoid blanking $uri, $host, etc.\\n\\n### 3. Update docker-compose.yml for backward compat\\n\\n- Add `BACKEND_HOST=admin` and `BACKEND_PORT=3001` environment variables to frontend service\\n- Existing docker-compose continues working with Docker DNS\\n\\n### 4. Set proxy timeouts for deep analysis\\n\\n- Add `proxy_read_timeout 120s` and `proxy_connect_timeout 10s` to nginx /api/ location block\\n- AI provider calls can take 30-60+ seconds; default 60s timeout is too tight\\n\\n### 5. Add backend health endpoint\\n\\n- Check if backend has a health endpoint; if not, add GET /api/health returning 200\\n- Railway needs this for health checks on the backend service\\n\\n### 6. Files to create/modify\\n\\n- `frontend/nginx.conf` → Rename to `frontend/nginx.conf.template`, replace admin hostname with `${BACKEND_HOST}:${BACKEND_PORT}`\\n- `frontend/Dockerfile` → Add envsubst runtime step, set default env vars\\n- `docker-compose.yml` → Add BACKEND_HOST/BACKEND_PORT env vars to frontend service\\n- `admin/src/index.ts` → Add /api/health endpoint if missing\\n\\n## Corner Cases (SRE)\\n- envsubst without variable list blanks nginx $uri/$host → MUST use `envsubst '${BACKEND_HOST} ${BACKEND_PORT}'`\\n- BACKEND_HOST not set → defaults to `admin` (docker-compose compat)\\n- Backend not started when frontend starts → 502 on /api/*, acceptable for Railway (retries on refresh)\\n- AI calls timing out → proxy_read_timeout 120s handles long-running analysis\\n- Backend must be internal-only on Railway (no public domain assigned)\\n\\n## Success Criteria\\n- [ ] envsubst only replaces BACKEND_HOST and BACKEND_PORT (not nginx vars like $uri)\\n- [ ] Frontend Dockerfile builds and serves SPA with templated nginx config\\n- [ ] nginx proxies /api/* to configurable backend host\\n- [ ] proxy_read_timeout set to 120s for deep analysis calls\\n- [ ] docker-compose.yml still works locally (backward compatible, BACKEND_HOST=admin)\\n- [ ] Backend has /api/health endpoint for Railway health checks\\n- [ ] Both Dockerfiles build successfully\"}","old_value":"{\"id\":\"prompt-strength-bz1\",\"title\":\"Add Railway configuration and update nginx proxy\",\"design\":\"## Goal\\nCreate Railway configuration so both services deploy from the same repo, and update nginx to proxy to the backend via Railway internal networking.\\n\\n## Implementation\\n\\n### 1. Study existing configs\\n- frontend/Dockerfile - multi-stage nginx build (works as-is)\\n- admin/Dockerfile - multi-stage Node build (works as-is)\\n- frontend/nginx.conf - needs proxy_pass update for Railway\\n\\n### 2. Create Railway config files\\n\\nRailway needs to know which Dockerfile to use for each service. Two approaches:\\n- railway.json at root (single service) — won't work for monorepo\\n- Per-service config via Railway dashboard + root directory setting\\n\\nFor a monorepo with two services, Railway uses the dashboard to set each service's root directory and Dockerfile path. No railway.json needed for basic setup.\\n\\nHowever, we should add a `railway.toml` or `nixpacks.toml` in each service directory if needed to override build/start commands.\\n\\n### 3. Update nginx.conf for Railway internal networking\\n\\nRailway provides internal networking via `RAILWAY_PRIVATE_DOMAIN` env var on each service. The backend service's internal URL will be `http://\\u003cbackend-private-domain\\u003e:3001`.\\n\\n**Problem:** nginx.conf is baked into the Docker image at build time, but the backend hostname is only known at runtime.\\n\\n**Solution:** Use envsubst in the nginx Dockerfile to template the backend URL at container startup. This is a standard nginx Docker pattern.\\n\\n### 4. Files to create/modify\\n\\n- `frontend/nginx.conf` → Convert `admin` hostname to env var `${BACKEND_HOST}`\\n- `frontend/Dockerfile` → Add envsubst step to template nginx.conf at runtime\\n- `frontend/nginx.conf.template` → Template version with `${BACKEND_HOST}` placeholder\\n\\n### 5. Verification\\n- Docker build still works locally with docker-compose (BACKEND_HOST=admin)\\n- Railway deploys both services\\n- /api/analyze proxies correctly to backend\\n\\n## Success Criteria\\n- [ ] Frontend Dockerfile builds and serves SPA with templated nginx config\\n- [ ] nginx proxies /api/* to configurable backend host via BACKEND_HOST env var\\n- [ ] docker-compose.yml still works locally (backward compatible)\\n- [ ] Both Dockerfiles build successfully\",\"status\":\"open\",\"priority\":1,\"issue_type\":\"feature\",\"owner\":\"129630752+MRiless@users.noreply.github.com\",\"created_at\":\"2026-03-28T14:19:26Z\",\"created_by\":\"MRiless\",\"updated_at\":\"2026-03-28T14:19:26Z\"}"}
{"actor":"MRiless","comment":null,"created_at":"2026-03-28T10:25:35Z","event_type":"status_changed","id":4,"issue_id":"prompt-strength-bz1","new_value":"{\"status\":\"in_progress\"}","old_value":"{\"id\":\"prompt-strength-bz1\",\"title\":\"Add Railway configuration and update nginx proxy\",\"design\":\"## Goal\\nCreate Railway configuration so both services deploy from the same repo, and update nginx to proxy to the backend via Railway internal networking.\\n\\n## Implementation\\n\\n### 1. Update nginx.conf to use templated backend host\\n\\nConvert `frontend/nginx.conf` to `frontend/nginx.conf.template`:\\n- Replace `http://admin:3001` with `http://${BACKEND_HOST}:${BACKEND_PORT}`\\n- **CRITICAL:** Keep all nginx variables ($uri, $host, etc.) as-is — envsubst must only target our variables\\n\\n### 2. Update frontend Dockerfile for envsubst at runtime\\n\\nAdd to frontend/Dockerfile:\\n- Set default env vars: `BACKEND_HOST=admin`, `BACKEND_PORT=3001`\\n- Replace CMD with: `envsubst '${BACKEND_HOST} ${BACKEND_PORT}' \\u003c /etc/nginx/conf.d/default.conf.template \\u003e /etc/nginx/conf.d/default.conf \\u0026\\u0026 nginx -g 'daemon off;'`\\n- **CRITICAL:** The envsubst command MUST specify exact variable names to avoid blanking $uri, $host, etc.\\n\\n### 3. Update docker-compose.yml for backward compat\\n\\n- Add `BACKEND_HOST=admin` and `BACKEND_PORT=3001` environment variables to frontend service\\n- Existing docker-compose continues working with Docker DNS\\n\\n### 4. Set proxy timeouts for deep analysis\\n\\n- Add `proxy_read_timeout 120s` and `proxy_connect_timeout 10s` to nginx /api/ location block\\n- AI provider calls can take 30-60+ seconds; default 60s timeout is too tight\\n\\n### 5. Add backend health endpoint\\n\\n- Check if backend has a health endpoint; if not, add GET /api/health returning 200\\n- Railway needs this for health checks on the backend service\\n\\n### 6. Files to create/modify\\n\\n- `frontend/nginx.conf` → Rename to `frontend/nginx.conf.template`, replace admin hostname with `${BACKEND_HOST}:${BACKEND_PORT}`\\n- `frontend/Dockerfile` → Add envsubst runtime step, set default env vars\\n- `docker-compose.yml` → Add BACKEND_HOST/BACKEND_PORT env vars to frontend service\\n- `admin/src/index.ts` → Add /api/health endpoint if missing\\n\\n## Corner Cases (SRE)\\n- envsubst without variable list blanks nginx $uri/$host → MUST use `envsubst '${BACKEND_HOST} ${BACKEND_PORT}'`\\n- BACKEND_HOST not set → defaults to `admin` (docker-compose compat)\\n- Backend not started when frontend starts → 502 on /api/*, acceptable for Railway (retries on refresh)\\n- AI calls timing out → proxy_read_timeout 120s handles long-running analysis\\n- Backend must be internal-only on Railway (no public domain assigned)\\n\\n## Success Criteria\\n- [ ] envsubst only replaces BACKEND_HOST and BACKEND_PORT (not nginx vars like $uri)\\n- [ ] Frontend Dockerfile builds and serves SPA with templated nginx config\\n- [ ] nginx proxies /api/* to configurable backend host\\n- [ ] proxy_read_timeout set to 120s for deep analysis calls\\n- [ ] docker-compose.yml still works locally (backward compatible, BACKEND_HOST=admin)\\n- [ ] Backend has /api/health endpoint for Railway health checks\\n- [ ] Both Dockerfiles build successfully\",\"status\":\"open\",\"priority\":1,\"issue_type\":\"feature\",\"owner\":\"129630752+MRiless@users.noreply.github.com\",\"created_at\":\"2026-03-28T14:19:26Z\",\"created_by\":\"MRiless\",\"updated_at\":\"2026-03-28T14:20:55Z\"}"}
{"actor":"MRiless","comment":null,"created_at":"2026-03-28T10:32:36Z","event_type":"updated","id":5,"issue_id":"prompt-strength-bz1","new_value":"{\"notes\":\"COMPLETED: nginx.conf.template created with envsubst variables and proxy timeouts, Dockerfile updated to use official nginx template mechanism, docker-compose.yml updated with BACKEND_HOST/PORT, /api/health added to backend.\"}","old_value":"{\"id\":\"prompt-strength-bz1\",\"title\":\"Add Railway configuration and update nginx proxy\",\"design\":\"## Goal\\nCreate Railway configuration so both services deploy from the same repo, and update nginx to proxy to the backend via Railway internal networking.\\n\\n## Implementation\\n\\n### 1. Update nginx.conf to use templated backend host\\n\\nConvert `frontend/nginx.conf` to `frontend/nginx.conf.template`:\\n- Replace `http://admin:3001` with `http://${BACKEND_HOST}:${BACKEND_PORT}`\\n- **CRITICAL:** Keep all nginx variables ($uri, $host, etc.) as-is — envsubst must only target our variables\\n\\n### 2. Update frontend Dockerfile for envsubst at runtime\\n\\nAdd to frontend/Dockerfile:\\n- Set default env vars: `BACKEND_HOST=admin`, `BACKEND_PORT=3001`\\n- Replace CMD with: `envsubst '${BACKEND_HOST} ${BACKEND_PORT}' \\u003c /etc/nginx/conf.d/default.conf.template \\u003e /etc/nginx/conf.d/default.conf \\u0026\\u0026 nginx -g 'daemon off;'`\\n- **CRITICAL:** The envsubst command MUST specify exact variable names to avoid blanking $uri, $host, etc.\\n\\n### 3. Update docker-compose.yml for backward compat\\n\\n- Add `BACKEND_HOST=admin` and `BACKEND_PORT=3001` environment variables to frontend service\\n- Existing docker-compose continues working with Docker DNS\\n\\n### 4. Set proxy timeouts for deep analysis\\n\\n- Add `proxy_read_timeout 120s` and `proxy_connect_timeout 10s` to nginx /api/ location block\\n- AI provider calls can take 30-60+ seconds; default 60s timeout is too tight\\n\\n### 5. Add backend health endpoint\\n\\n- Check if backend has a health endpoint; if not, add GET /api/health returning 200\\n- Railway needs this for health checks on the backend service\\n\\n### 6. Files to create/modify\\n\\n- `frontend/nginx.conf` → Rename to `frontend/nginx.conf.template`, replace admin hostname with `${BACKEND_HOST}:${BACKEND_PORT}`\\n- `frontend/Dockerfile` → Add envsubst runtime step, set default env vars\\n- `docker-compose.yml` → Add BACKEND_HOST/BACKEND_PORT env vars to frontend service\\n- `admin/src/index.ts` → Add /api/health endpoint if missing\\n\\n## Corner Cases (SRE)\\n- envsubst without variable list blanks nginx $uri/$host → MUST use `envsubst '${BACKEND_HOST} ${BACKEND_PORT}'`\\n- BACKEND_HOST not set → defaults to `admin` (docker-compose compat)\\n- Backend not started when frontend starts → 502 on /api/*, acceptable for Railway (retries on refresh)\\n- AI calls timing out → proxy_read_timeout 120s handles long-running analysis\\n- Backend must be internal-only on Railway (no public domain assigned)\\n\\n## Success Criteria\\n- [ ] envsubst only replaces BACKEND_HOST and BACKEND_PORT (not nginx vars like $uri)\\n- [ ] Frontend Dockerfile builds and serves SPA with templated nginx config\\n- [ ] nginx proxies /api/* to configurable backend host\\n- [ ] proxy_read_timeout set to 120s for deep analysis calls\\n- [ ] docker-compose.yml still works locally (backward compatible, BACKEND_HOST=admin)\\n- [ ] Backend has /api/health endpoint for Railway health checks\\n- [ ] Both Dockerfiles build successfully\",\"status\":\"in_progress\",\"priority\":1,\"issue_type\":\"feature\",\"owner\":\"129630752+MRiless@users.noreply.github.com\",\"created_at\":\"2026-03-28T14:19:26Z\",\"created_by\":\"MRiless\",\"updated_at\":\"2026-03-28T14:25:35Z\"}"}
{"actor":"MRiless","comment":null,"created_at":"2026-03-28T10:32:55Z","event_type":"created","id":6,"issue_id":"prompt-strength-bbc","new_value":"","old_value":""}
